# 交接提示语 · Handoff Prompt

> 给 Codex（或任何接手的 agent）。可整段粘贴。

---

你接手一个叫 **文心 · 万形 (Wenxin · Wanxing)** 的双语设计系统仓库。
它刚完成一次彻底重构：从「只有规范正文」变成「规范 + 可复用产物」。
你的任务是**审计、补充、完善**，并且**与四个已上线的真实站点做对账**。

## 先读这些，不要先动手

| 顺序 | 文件 | 为什么 |
|---|---|---|
| 1 | `CLAUDE.md` | 仓库怎么运作、三个检查脚本各自抓什么 |
| 2 | `spec/README.md` | 规范总入口 |
| 3 | **`spec/tracks.md`** | **双轨仲裁 —— 本系统最核心的机制，冲突时的判定规则** |
| 4 | `spec/soul/philosophy.md` | 七维度模型；哪些维度不可变 |
| 5 | `spec/DECISIONS.md` | 已经做过的九条判定与理由 |
| 6 | `spec/forms/DECISIONS-MATRIX.md` | 九形态各自被迫做的不同决定 |
| 7 | `spec/PROVENANCE.md` | 历史材料在 git `93fba710`，删除前已取材 |

跑一遍确认基线是绿的：

```sh
npm run build && npm run check
python3 -m http.server 8899     # 然后看 examples/gallery/ 与 examples/
```

**当前基线：九形态示例 0 失败 0 警告。任何你的改动都不应打破它。**

---

## 任务一 · 与四个真实站点对账（这是重点）

以下四个站点**已经基于文心构建，但站主后续做过调整**：

- `zopiya.com`
- `blog.zopiya.com`
- `note.zopiya.com`
- `garden.zopiya.com`

它们是**田野证据**，不是待纠正的偏差。请逐站考察：token 取值、排版节奏、组件形态、
导航模式、动效、暗色模式、无障碍表现，并与 `spec/` 和 `kit/` 逐条比对。

**每一处差异必须归入三类之一**，并写进 `spec/DECISIONS.md`（沿用现有 D-n 格式：
冲突是什么 → 判定 → 属于 tracks.md 的哪一层 → 为什么）：

| 类别 | 含义 | 处理 |
|---|---|---|
| **A · 实践比规范好** | 站点的做法更对，规范落后了 | **改规范和 kit**，站点是对的 |
| **B · 合法的形态适配** | 差异由媒介/轨道决定，两边都对 | 写进对应 `spec/forms/fN-*.md`，说明边界 |
| **C · 漂移** | 站点偏离且无正当理由 | 记录下来，给出回归建议；**不要擅自改站点** |

判 A 类时要克制：**站点跑在真实读者面前，它的经验值通常比纸面推演更可信。**
但如果某处差异动到了[不变之魂](spec/soul/philosophy.md)（A9 克制之美 / B9 温暖极简 / D9 暖土调），
先当成 C 类提出来讨论，不要直接改规范。

特别留意这几处 —— 它们最可能已经在实践中被调整：

- **accent 预算**（≤2 处/页）在真实内容站上是否守得住？F7 已有形态例外（`spec/DECISIONS.md` D-5）。
- **`wx-entry` 是否真的顶替了卡片**？若站点上又长出了方框，说明替代品不够好用，
  这是 kit 的问题，不是站点的问题（见 D-4 的推理）。
- **语义色**是新加的（D-1/D-2），站点上很可能还是旧的或者根本没有。
- **暗色模式**：`kit/tokens/dark.css` 是这次才落地的，站点的暗色方案可能是各自实现的。
- **字体加载**：kit 用自托管 + `unicode-range` 子集化；站点若还在用 CDN，
  对照准则 M-6 / M-8 评估。

产出：一份 `spec/FIELD-AUDIT.md`（中英双语，沿用仓库写作约定），
按站点分节，每条差异标注 A/B/C 与依据。

---

## 任务二 · 补完已知的缺口

以下是**确实未完成**的部分，不用你去发现：

1. **`kit/patterns/`（未建）** —— 五种[页面原型](spec/forms/page-archetypes.md) A–E 的骨架。
   四个真实站点正好覆盖阅读型/列表型/展示型，是很好的取材来源。
2. **`kit/dist/`（未建）** —— 目前 `kit/index.css` 靠 `@import` 链加载。
   需要 `build:css` 把它拼成单文件，并按形态出 `wenxin-fN.css`。**产物不压缩**（已定）。
3. **`kit/components/react/`（未建）** —— 可选薄封装，纯 `.jsx` 不带构建步骤。**可以推迟**。
4. **审计器有落差** —— 契约里声明了约 21 个 `profileSpecificChecks`
   （`sidebar` `toc` `search` `touch-targets` `cmyk-mapping` `figure-numbering` …），
   但 `scripts/render-audit.mjs` 只实现了约 12 个（F4/F5/F7/F8/F9 的部分）。
   **契约在承诺一些根本不会运行的检查** —— 要么实现，要么从契约里删掉。
   按本仓库的惯例：坦白标注，不要抹平。
5. **两份未消费的取材件** —— `spec/_harvest/component-evidence.md`（v2 组件的证据）
   与 `mermaid-translation-rules.md`（F8 生成主题的输入）。消费完即删除。
6. **v2 组件约 18 个** —— 清单见 `spec/soul/components.md`。
   优先做四个站点真的用得到的，不要为凑数而做。

---

## 硬性约束 · 不要做的事

- **不要重新引入 `opendesign/`**。Open Design 适配是日后从本仓库**派生出去**的另一个项目
  （判定见 D-8）。
- **不要让卡片以别的名字回来** —— `.panel` `.box` `.tile` `.surface` 都是同一个东西。
  判据是几何形状：四边包围的方框（边框/阴影/填充底色任一 + 圆角）。
- **不要动 A9 / B9 / D9**。其余维度（C 排版、E 动效、F 形态、G 文化语境）可以适配。
- **不要为了让检查通过而放宽检查**。检查器抓到你，通常说明代码错了；
  如果确实是误报，修检查器并**用一个故意违规的样本验证修完还抓得住**
  （`scripts/check-forbidden.mjs` 与 `check-tokens.mjs` 都这样验证过）。
- **不要把矛盾抹平**。本仓库的惯例是坦白标注 ——
  参见 `spec/forms/cross-form-matrix.md` 里如实记下自己此前的误判。
- **改 token 后必须 `npm run build` 并提交生成物**；改 `kit/` 后必须 `npm run check`。

## 一个会浪费你十分钟的坑

改完 CSS 浏览器没反应，几乎一定是缓存了 `kit/index.css`。
加查询串强制重取：`kit/index.css?v=2`。

## 验收

```sh
npm run check     # 必须保持 0 失败 0 警告
```

外加：新增或修改的页面在浏览器里**亮色与暗色都看一遍**。
本次重构中，三个真实缺陷是「量出来」而不是「看出来」的
（两处对比度不达 AA、两个语义色色相只差 4°）——
涉及颜色时请实际计算对比度与色差，不要凭肉眼判断。
