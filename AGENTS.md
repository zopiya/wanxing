# AGENTS.md

给在**这个仓库里干活**的 agent。想在**别的项目里用这套设计系统**，读
[`site/llms.txt`](./site/llms.txt) 和 [`kit/wenxin.json`](./kit/wenxin.json)，不是这个文件。

## 先读

完整的工作约定在 [`CLAUDE.md`](./CLAUDE.md)，不在这里重复。**开工前必读它。**
最少要带走三件事：

1. **`spec/tracks.md` 的三层仲裁**是解决任何"设计哲学 vs 主流惯例"冲突的唯一途径。
   不要临时争论，按层判。
2. **`kit/tokens/core.css` 是所有数值的唯一真源。** `.md` 里的数值是摘录，冲突时
   `core.css` 赢。绝不把 Markdown 里的值手抄进代码，也绝不只改散文里的 token。
3. **`site/*.html` 是生成物。** 改 `site/_pages/<slug>.html`，在 `site/_nav.json` 登记，
   跑 `npm run build:site`。

## 命令

```sh
npm run build     # tokens + 图表主题 + 契约包 + CSS 包 + 文档站 + llms.txt
npm run check     # 全套质量门
npm run audit -- dist/<slug>/index.html   # 审计合同化消费者输出；路径和合同均须匹配
python3 -m http.server 8899   # 示例需要 http；file:// 会挡住 @import 和字体
```

改了 token 就跑 `npm run build` 并提交重新生成的文件；改了 `kit/` 下任何东西，
提交前跑 `npm run check`。

## 三条硬约束

**不变之魂**：`A9 克制之美` / `B9 温暖极简` / `D9 暖土调`。改动它们，产物就不再属于这套
语言。你若认为必须改，**写进 `spec/DECISIONS.md` 作为待议项，不要单方面改**。

**两种排除不是一回事**（D-23）。卡片、spinner、骨架屏、自动轮播、水印是**哲学性排除**——
它们是这套系统的身份，不因范围扩大而回来。滑块、上传、日期、树、气泡曾因**范围**被排除，
范围扩大后已经实现。拒绝一个组件请求之前，先分清是哪一种。

**没有卡片组件。** 用 `wx-entry`——单边线加留白。禁令针对的是*几何形状*（四边包围：边框、
阴影或填充 + 圆角），不是那个名词；不要以 `.panel` `.box` `.tile` `.surface` 重新引入。

## 检查这件事本身

**一个不可能失败的检查比没有检查更糟**——它的绿色会被当成证据引用。这个仓库有过三次
"绿色掩盖真实缺陷"：审计矩阵读错字段、五个文字 token 不达 AA 而无人测量、对比度只对
一个底色测而代码块两个主题都不合格。

所以：**你新加的每一个检查，都要先故意制造它该抓的缺陷，确认它真的失败，再声称通过。**
提交信息里写明怎么验的。

`kit/contracts/forbidden.json` 里的 `judgement` 数组是不可机械检查的规则，没有任何东西
读它。这是有意的——**一条没被标出来的不可执行规则，会被误以为有人在守**。

## 度量与提交

每个测量断言都要附可复现的命令或代码。审计你的人应该能**跑一遍**验证，而不是**重做一遍**。

分阶段提交，每个提交自成一体，信息里写清**判断理由**而不只是改了什么。

**坦白记录矛盾与未完成项，不要抹平。** 这是这个仓库的明确约定：`spec/DECISIONS.md` 的
D-26 写下了一次取舍的代价而不是掩盖它。诚实的缺口记录比虚假的完整清单有价值得多。
